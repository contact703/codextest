# Amostra pública

Esta pasta contém um roteiro esperado para o clipe de domínio público "Big Buck Bunny". Use `download_sample.sh` para baixar o vídeo e compare os resultados gerados pelo pipeline com os arquivos `expected_AD.srt` e `expected_AD.txt`.
